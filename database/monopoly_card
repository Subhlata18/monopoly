DROP TABLE IF EXISTS `Cards`;

CREATE TABLE Cards (
    id INT AUTO_INCREMENT PRIMARY KEY,
    card_type ENUM('Chance', 'Community Chest') NOT NULL,
    description VARCHAR(255) NOT NULL,
    action VARCHAR(255) -- You can adjust the data type as needed
);
